export interface RelayHealth {id:string;region:string;endpoint:string;latencyMs:number;loadPercent:number;available:boolean;lastHeartbeat:Date;}
export interface PathCandidate {kind:"DIRECT"|"LOCAL_RELAY"|"REGIONAL_RELAY";endpoint:string;score:number;}
export class RelayRoutingService {select(directEndpoint:string|undefined,localRelay:RelayHealth|undefined,regional:RelayHealth[],now=new Date()):PathCandidate[]{const out:PathCandidate[]=[];if(directEndpoint)out.push({kind:"DIRECT",endpoint:directEndpoint,score:1000});if(localRelay&&this.healthy(localRelay,now))out.push({kind:"LOCAL_RELAY",endpoint:localRelay.endpoint,score:800-localRelay.latencyMs-localRelay.loadPercent});for(const r of regional.filter(x=>this.healthy(x,now)))out.push({kind:"REGIONAL_RELAY",endpoint:r.endpoint,score:600-r.latencyMs-r.loadPercent});return out.sort((a,b)=>b.score-a.score);}private healthy(r:RelayHealth,now:Date){return r.available&&now.getTime()-r.lastHeartbeat.getTime()<45_000&&r.loadPercent<95;}}
